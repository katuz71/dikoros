# CORS — список разрешённых origins (через запятую)
STORE_CORS=http://localhost:8000
ADMIN_CORS=http://localhost:5173,http://localhost:9000
AUTH_CORS=http://localhost:5173,http://localhost:9000,http://localhost:8000

# Redis — кэш и очереди
REDIS_URL=redis://localhost:6379

# Database — PostgreSQL 16
DATABASE_URL=postgres://medusa:medusa_pass@localhost:5432/medusa_db

# Secrets — СГЕНЕРИРУЙ сам через: openssl rand -hex 32
# НИКОГДА не коммить реальные значения в .env.template или .env
JWT_SECRET=
COOKIE_SECRET=

# Redis infrastructure modules
CACHE_REDIS_URL=redis://localhost:6379
EVENTS_REDIS_URL=redis://localhost:6379
WE_REDIS_URL=redis://localhost:6379
